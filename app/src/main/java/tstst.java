import android.content.Context;
import android.graphics.Canvas;
import android.widget.Button;

/**
 * Created by Ahmad on 29 ديس، 14 م.
 */
public class tstst extends Button {
    public tstst(Context context) {
        super(context);
    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
    }
}
